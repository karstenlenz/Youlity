import React from 'react'
import MotiveButtons from './MotiveButtons'

export default {
  title: 'Motive Buttons',
  component: MotiveButtons,
}

export const MotiveButtonsComponent = () => (
  <MotiveButtons handleMotiveClick={() => ''} />
)
